# 优化首页文案

## 流程状态

- 当前阶段：execute（已完成）
- 最近动作：所有任务执行完毕，tsc --noEmit 验证通过
- 更新时间：2025-05-13

## 背景

- 基本完成了落地页的样式与交互 @src/landing/
- i18n 配置 @src/i18n/locales/

## 需求

- @src/i18n/locales/zh.ts L34-L35
  - 合并两个文案配置
  - 文案替换为：立即开始全新视频编辑体验
- @src/i18n/locales/zh.ts L47-L49
  - 文案替换为：复制、~~删除~~、重排文字，等同于编辑视频
- 英文也需同步修改
- @src/demo/AssetManagerModal.tsx L20-L25
  - 这里的标签内容使用英文即可，不需要国际化配置

## 现状分析

### 需求 1：demo 标题合并（zh.ts L34-L35 / en.ts L35-L36）

当前使用两个 i18n key 组合渲染标题：

- `demo.title1`：普通文本部分
- `demo.titleEm`：斜体强调部分（`<em>` 包裹、accent 颜色）

渲染代码位于 `src/landing/DemoSection.tsx:18-20`：

```tsx
{t("demo.title1")}{" "}
<em class="italic text-[--color-accent]">{t("demo.titleEm")}</em>
```

需求要求合并为单一文案 "立即开始全新视频编辑体验"，不需要 accent 颜色强调，简化为单个 i18n key。

### 需求 2：illust 文案替换（zh.ts L47-L49 / en.ts L48-L51）

当前渲染结构（`src/landing/TimelineIllustration.tsx:117-124`）：

```
pre + <strike>strike</strike> + <highlight>highlight</highlight> + post
```

需改为："复制、~~删除~~、重排文字，等同于编辑视频"

- pre: "复制、"
- strike: "删除"（带删除线）
- highlight: ""（留空）
- post: "、重排文字，等同于编辑视频。"

英文："Copy, ~~delete~~, reorder text = edit video."

### 需求 3：英文同步

en.ts 需对应修改 demo 标题和 illust textB。

### 需求 4：AssetManagerModal 标签硬编码（L20-25）

使用英文硬编码标签，移除 i18n `t()` 调用，修正 `text` 类型映射。

## 技术实现方案

### 方案 1：demo 标题合并

1. zh.ts：删除 `demo.title1` 和 `demo.titleEm`，新增 `demo.title` = "立即开始全新视频编辑体验"
2. en.ts：同步删除两个 key，新增 `demo.title` = "Start a brand new video editing experience"
3. DemoSection.tsx：将标题渲染简化为 `{t("demo.title")}`，移除 `<em>` 包裹

### 方案 2：illust textB 文案替换

1. zh.ts 更新四个 key 的值
2. en.ts 同步更新对应英文
3. TimelineIllustration.tsx 无需改动

### 方案 3：AssetManagerModal 标签硬编码

1. `TYPE_LABEL_KEYS` 重命名为 `TYPE_LABELS`，值改为英文硬编码
2. 修正 `text` → "Text"
3. 移除 `t()` 调用，直接使用 `TYPE_LABELS[...]`

## 待确认问题

- 暂无

## 任务清单

- [x] 任务 1：更新 zh.ts — 删除 demo.title1/demo.titleEm，新增 demo.title；更新 illust.textB.pre/strike/highlight/post 四个值
- [x] 任务 2：更新 en.ts — 删除 demo.title1/demo.titleEm，新增 demo.title；更新 illust.textB 四个对应英文值
- [x] 任务 3：更新 DemoSection.tsx — 标题渲染改为单 key，移除 em 包裹
- [x] 任务 4：更新 AssetManagerModal.tsx — TYPE_LABEL_KEYS 改为 TYPE_LABELS 英文硬编码，移除 t() 调用，修正 text 映射
- [x] 任务 5：执行构建验证，确认无报错

## 执行记录

- 任务 1：zh.ts — 合并 demo.title1+demo.titleEm → demo.title="立即开始全新视频编辑体验"；illust.textB 四个值已替换。验证：tsc 通过。
- 任务 2：en.ts — 合并 → demo.title="Start a brand new video editing experience"；illust.textB 英文已同步。验证：tsc 通过。
- 任务 3：DemoSection.tsx — 标题渲染简化为 `{t("demo.title")}`，移除 em 包裹。验证：tsc 通过。
- 任务 4：AssetManagerModal.tsx — TYPE_LABEL_KEYS → TYPE_LABELS 英文硬编码（Video/Audio/Image/Text），移除 t() 调用，修正 text 映射。验证：tsc 通过。
- 任务 5：`npx tsc --noEmit` 执行通过，无报错。
